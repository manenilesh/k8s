apiVersion: v1
kind: ConfigMap
metadata: 
  name: my-configmap-app
  labels:
    app: my-cm-app
spec:
  template:
    metadata:
      name:  my-cm-app
      labels: 
        app: my-cm-app
    spec:
      containers:
        - name:  my-cm-pod 
          image:  image
          name:  my-name
          env:
            - name:  ENVVARNAME
              value:  ENVVARVALUE